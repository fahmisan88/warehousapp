<nav class="navbar navbar-default">
    <div class="container-fluid">
      <div class="navbar-header">
        <a class="navbar-brand" href="<%= root_path %>" target="_self">EZICARGO</a>
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#header-dropdown" aria-expanded="false">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
      </div>

      <div class="collapse navbar-collapse" id="header-dropdown">
        <ul class="nav navbar-nav navbar-right">
          <li><a id="butt" style="cursor:pointer;">How It Works<span class="sr-only">(current)</span></a></li>
          <%if current_user && current_user.status == "Active" && current_user.address?%>
          <li><a href="<%=dashboards_path%>">DASHBOARD<span class="sr-only">(current)</span></a></li>
          <li><%= link_to "Logout", session_path(current_user), method: :delete, class: 'white shadowed link-styling' %><span class="sr-only">(current)</span></li>
          <%elsif current_user && current_user.status == "Active" && current_user.address.nil?%>
          <li><%= link_to "Logout", session_path(current_user), method: :delete, class: 'white shadowed link-styling' %><span class="sr-only">(current)</span></li>
          <%else%>
          <li><a href="/login" target="_self">Log In<span class="sr-only">(current)</span></a></li>
          <li><a href="/register" target="_self">Register<span class="sr-only">(current)</span></a></li>
          <%end%>
        </ul>
      </div>
    </div>
</nav>
